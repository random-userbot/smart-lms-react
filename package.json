{
  "name": "smartlms",
  "private": true,
  "workspaces": ["smartlms-frontend"],
  "scripts": {
    "dev": "npm run dev --workspace=smartlms-frontend",
    "build": "npm run build --workspace=smartlms-frontend",
    "preview": "npm run preview --workspace=smartlms-frontend"
  }
}
